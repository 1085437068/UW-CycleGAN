1.运行test_cycle.py
（1）疑问：
--clean-dir: 指定清晰图像数据集的目录路径。通常用于存储未受干扰或原始质量的图像。
--depth-dir: 指定深度图数据集的目录路径。深度图通常用于表示场景中每个像素点到相机的距离。
--underwater-dir: 指定水下图像数据集的目录路径。这些图像通常是受水下环境影响的图像。
--underwater-dir-refer: 指定水下图像参考数据集的目录路径。这个目录中的图像可以作为参考，用于评估模型的性能或进行对比。
 
 个人理解：在运行test的时候只需要underwater-dir的路径正确，其他三个文件夹是不需要的，运行train的时候才需要四个文件夹。

 2.运行train_cycle.py
 （1）修改下预训练权重路径：
    parser.add_argument('--weights_A', type=str, default='model/model_A.pt', help='initial weights path')
    parser.add_argument('--weights_d', type=str, default='model/model_d.pt', help='initial weights path')
    parser.add_argument('--weights_t', type=str, default='model/model_t.pt', help='initial weights path')
    parser.add_argument('--weights_b', type=str, default='model/model_b.pt', help='initial weights path')

（2）使用自己的数据集重新训练的话，需要有四个数据集
疑问：如果要使用underwater-dir-refer的话，不是还要paired的数据集么？怎么还能叫做无监督呢？

(3)训练后保存的模型在哪？
答：在runs/train/exp1/weights文件夹下，这个文件夹还保存了中间过程的训练图